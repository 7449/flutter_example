import 'package:flutter/material.dart';

/// 未知
class UnKnowScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('UnKnow')),
    );
  }
}
